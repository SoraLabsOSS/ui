# Marketing site (apps/web) — defaults to localhost:3001 if unset
NEXT_PUBLIC_SITE_URL="http://localhost:3001"

# Docs app (apps/www) — defaults to localhost:3000 if unset
NEXT_PUBLIC_DOCS_URL="http://localhost:3000"

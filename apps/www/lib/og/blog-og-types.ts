/** OG card content for `/blog` and `/blog/[slug]` preview images. */
export interface BlogOgContent {
  author: string;
  avatar?: string;
  description?: string;
  handle: string;
  quote: string;
}

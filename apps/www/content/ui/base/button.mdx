---
title: Button
description: An accessible button component built on Base UI and Motion for React with shadcn/ui variants, comprehensive sizes, and physics-based hover/tap scaling.
author:
  name: Axyl
  url: https://github.com/Axyl1410
intent: Trigger an immediate user action with clear visual and motion feedback.
role: action
a11yConstraints:
  - Preserve native button keyboard activation and disabled semantics.
  - Keep loading and disabled states announced by the button label.
motionEngine: Motion spring scale on hover and tap with reduced-motion bypass.
compositionRules:
  - Use as the trigger for dialogs, menus, and form submission.
  - Keep destructive actions explicit with the destructive variant.
compositionRecipes:
  - name: dialog-trigger
    description: Open a modal from a semantic button trigger.
    components:
      - base/button
      - base/dialog
    constraints:
      - Use DialogTrigger render rather than nesting interactive elements.
---

<ComponentPreview name="demo-button" description="Default primary button with Motion spring scaling on hover and tap." />

## Installation

<ComponentInstallation name="base-button" />

## Usage

```tsx
import { Button } from "@/components/sora-ui/base/button";

export default function Page() {
  return <Button>Button</Button>;
}
```

## Examples

### Variants

<ComponentPreview name="demo-button-variants" description="All visual button variants: default, secondary, destructive, outline, ghost, and link." />

### Sizes

<ComponentPreview name="demo-button-sizes" description="Button size presets from extra small (xs) to large (lg)." />

### With Icon

<ComponentPreview name="demo-button-with-icon" description="Button combined with leading or trailing Lucide icons." />

### Loading State

<ComponentPreview name="demo-button-loading" description="Disabled button showing an animated spinner." />

### Custom Motion Scaling

<ComponentPreview name="demo-button-motion" description="Customizable hoverScale and tapScale multipliers, with option to disable scaling." />

## Props

<TypeTable
  type={{
    variant: {
      description: "Visual variant of the button.",
      type: "\"default\" | \"outline\" | \"secondary\" | \"ghost\" | \"destructive\" | \"link\"",
      default: "\"default\"",
    },
    size: {
      description: "Size preset of the button.",
      type: "\"default\" | \"xs\" | \"sm\" | \"lg\" | \"icon\" | \"icon-xs\" | \"icon-sm\" | \"icon-lg\"",
      default: "\"default\"",
    },
    hoverScale: {
      description: "Motion scale multiplier on pointer hover.",
      type: "number",
      default: "1.05",
    },
    tapScale: {
      description: "Motion scale multiplier on pointer tap/click.",
      type: "number",
      default: "0.95",
    },
    disableAnimation: {
      description: "Whether to disable hover and tap scaling animation.",
      type: "boolean",
      default: "false",
    },
    className: {
      description: "Additional CSS classes merged onto the button element.",
      type: "string",
    },
  }}
/>

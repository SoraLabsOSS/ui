---
title: Dialog
description: An accessible modal dialog built on Base UI with smooth Motion spring entrance and exit animations.
author:
  name: Axyl
  url: https://github.com/Axyl1410
---

<ComponentPreview name="dialog" description="Interactive dialog showcasing profile editing and confirmation alert modal patterns." />

## Installation

<ComponentInstallation name="dialog" />

## Usage

### Basic Usage

Assemble the dialog with `Dialog`, `DialogTrigger`, `DialogContent`, `DialogHeader`, `DialogTitle`, and `DialogDescription`:

```tsx
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/sora-ui/base/dialog";

export default function BasicDialog() {
  return (
    <Dialog>
      <DialogTrigger className="inline-flex h-9 items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground">
        Open Dialog
      </DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Notification Preferences</DialogTitle>
          <DialogDescription>
            Manage how you receive alerts and account updates.
          </DialogDescription>
        </DialogHeader>
        <p className="text-sm text-muted-foreground">
          Your settings have been synchronized across all devices.
        </p>
      </DialogContent>
    </Dialog>
  );
}
```

### Form Action with Footer

Add action buttons and inputs inside `DialogContent` and `DialogFooter`:

```tsx
import {
  Dialog,
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/sora-ui/base/dialog";

export function EditProfileDialog() {
  return (
    <Dialog>
      <DialogTrigger className="inline-flex h-9 items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground">
        Edit Profile
      </DialogTrigger>
      <DialogContent className="sm:max-w-[425px]">
        <DialogHeader>
          <DialogTitle>Edit profile</DialogTitle>
          <DialogDescription>
            Make changes to your profile here. Click save when you're done.
          </DialogDescription>
        </DialogHeader>
        <div className="grid gap-4 py-4">
          <div className="grid grid-cols-4 items-center gap-4">
            <label htmlFor="name" className="text-right text-sm font-medium">
              Name
            </label>
            <input
              id="name"
              defaultValue="Alex Rivera"
              className="col-span-3 h-9 rounded-md border border-input bg-background px-3 py-1 text-sm shadow-xs"
            />
          </div>
        </div>
        <DialogFooter showCloseButton>
          <button
            type="submit"
            className="inline-flex h-9 items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground"
          >
            Save changes
          </button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

### Destructive Confirmation Modal

```tsx
import {
  Dialog,
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/sora-ui/base/dialog";

export function ConfirmDeleteDialog() {
  return (
    <Dialog>
      <DialogTrigger className="inline-flex h-9 items-center justify-center rounded-md border border-input bg-background px-4 py-2 text-sm font-medium hover:bg-muted">
        Delete Repository
      </DialogTrigger>
      <DialogContent className="sm:max-w-[400px]">
        <DialogHeader>
          <DialogTitle>Are you absolutely sure?</DialogTitle>
          <DialogDescription>
            This action cannot be undone. This will permanently delete your repository and remove all collaborators.
          </DialogDescription>
        </DialogHeader>
        <DialogFooter>
          <DialogClose className="inline-flex h-9 items-center justify-center rounded-md border border-input bg-background px-4 py-2 text-sm font-medium">
            Cancel
          </DialogClose>
          <button
            type="button"
            className="inline-flex h-9 items-center justify-center rounded-md bg-destructive px-4 py-2 text-sm font-medium text-destructive-foreground"
          >
            Confirm Delete
          </button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

## Props

### Dialog

<TypeTable
  type={{
    open: {
      description: "Controlled open state of the dialog.",
      type: "boolean",
    },
    defaultOpen: {
      description: "Initial open state when uncontrolled.",
      type: "boolean",
      default: "false",
    },
    onOpenChange: {
      description: "Event handler called when open state changes.",
      type: "(open: boolean, eventDetails: Dialog.Root.ChangeEventDetails) => void",
    },
    dismissible: {
      description: "Whether clicking the backdrop or pressing Escape closes the dialog.",
      type: "boolean",
      default: "true",
    },
    modal: {
      description: "Whether the dialog traps focus and prevents interaction with the rest of the page.",
      type: "boolean",
      default: "true",
    },
  }}
/>

### DialogContent

<TypeTable
  type={{
    showCloseButton: {
      description: "Whether to render the corner X close icon button.",
      type: "boolean",
      default: "true",
    },
    overlayClassName: {
      description: "Custom CSS class applied to the backdrop overlay.",
      type: "string",
    },
    className: {
      description: "Class names merged onto the dialog popup container.",
      type: "string",
    },
  }}
/>

## Credits

<ComponentCredits name="dialog" />

---
title: Dialog
description: An accessible modal dialog built on Base UI with smooth Motion spring 3D entrance and exit animations.
author:
  name: Axyl
  url: https://github.com/Axyl1410
---

<ComponentPreview name="demo-dialog" description="Interactive modal dialog with 3D perspective rotation, form inputs, and Motion spring exit transitions." />

## Installation

<ComponentInstallation name="dialog" />

## Usage

```tsx
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/sora-ui/base/dialog";

export default function BasicDialog() {
  return (
    <Dialog>
      <DialogTrigger className="inline-flex h-9 items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground">
        Open Dialog
      </DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Notification Preferences</DialogTitle>
          <DialogDescription>
            Manage how you receive alerts and account updates.
          </DialogDescription>
        </DialogHeader>
        <p className="text-sm text-muted-foreground">
          Your settings have been synchronized across all devices.
        </p>
      </DialogContent>
    </Dialog>
  );
}
```

## Examples

### Share Link

<ComponentPreview name="demo-dialog-share" description="Dialog with a copyable project link and custom Done close button." />

### Destructive Confirmation

<ComponentPreview name="demo-dialog-destructive" description="Destructive alert confirmation modal for irreversible actions." />

### Scrollable Body

<ComponentPreview name="demo-dialog-scrollable" description="Dialog with scrollable content container and fixed header/footer actions." />

## Props

### Dialog

<TypeTable
  type={{
    open: {
      description: "Controlled open state of the dialog.",
      type: "boolean",
    },
    defaultOpen: {
      description: "Initial open state when uncontrolled.",
      type: "boolean",
      default: "false",
    },
    onOpenChange: {
      description: "Event handler called when open state changes.",
      type: "(open: boolean, eventDetails: Dialog.Root.ChangeEventDetails) => void",
    },
    dismissible: {
      description: "Whether clicking the backdrop or pressing Escape closes the dialog.",
      type: "boolean",
      default: "true",
    },
    modal: {
      description: "Whether the dialog traps focus and prevents interaction with the rest of the page.",
      type: "boolean",
      default: "true",
    },
  }}
/>

### DialogContent

<TypeTable
  type={{
    showCloseButton: {
      description: "Whether to render the corner X close icon button.",
      type: "boolean",
      default: "true",
    },
    overlayClassName: {
      description: "Custom CSS class applied to the backdrop overlay.",
      type: "string",
    },
    containerClassName: {
      description: "Custom CSS class applied to the fixed wrapper container.",
      type: "string",
    },
    className: {
      description: "Class names merged onto the dialog popup container.",
      type: "string",
    },
  }}
/>

---
title: Checkbox
description: An accessible, stylable checkbox component built with Radix UI and Motion for React with spring-animated checkmark SVG drawing.
author:
  name: Axyl
  url: https://github.com/Axyl1410
intent: Toggle an independent boolean setting with Radix state semantics.
role: input
a11yConstraints:
  - Preserve the checkbox role, label association, and keyboard toggling.
  - Do not use a checkbox for mutually exclusive choices.
motionEngine: Motion checkmark and scale feedback synchronized with Radix checked state.
compositionRules:
  - Pair with a visible label and description where additional context is needed.
  - Keep form submission semantics explicit when using custom render elements.
compositionRecipes:
  - name: radix-preference
    description: Add an accessible preference control to a form.
    components:
      - radix/checkbox
      - radix/button
    constraints:
      - Keep the checkbox label and submit action independently reachable.
---

<ComponentPreview name="demo-radix-checkbox" description="Default animated Radix UI checkbox with interactive label and spring checkmark draw animation." />

## Installation

<ComponentInstallation name="radix-checkbox" />

## Usage

```tsx
import { Checkbox } from "@/components/sora-ui/radix/checkbox";

export default function Page() {
  return <Checkbox label="Accept terms and conditions" />;
}
```

### Uncontrolled with Default Checked

```tsx
import { Checkbox } from "@/components/sora-ui/radix/checkbox";

export default function Page() {
  return <Checkbox defaultChecked label="Enable email notifications" />;
}
```

### Controlled

```tsx
import { Checkbox } from "@/components/sora-ui/radix/checkbox";
import { useState } from "react";

export default function Page() {
  const [checked, setChecked] = useState(false);

  return (
    <Checkbox
      checked={checked}
      onCheckedChange={(val) => setChecked(Boolean(val))}
      label="Controlled acceptance"
    />
  );
}
```

## Examples

### With Helper Text

<ComponentPreview name="demo-radix-checkbox-with-text" description="Checkbox with supplementary title and description text." />

### Card Selector

<ComponentPreview name="demo-radix-checkbox-card" description="Selectable card container with embedded animated checkbox." />

### Indeterminate State

<ComponentPreview name="demo-radix-checkbox-indeterminate" description="Checkbox in a partially checked indeterminate state." />

## Props

<TypeTable
  type={{
    checked: {
      description: "The controlled checked state of the checkbox.",
      type: "boolean | \"indeterminate\"",
    },
    defaultChecked: {
      description: "The initial checked state when uncontrolled.",
      type: "boolean | \"indeterminate\"",
      default: "false",
    },
    onCheckedChange: {
      description: "Event handler called when the checked state changes.",
      type: "(checked: boolean | \"indeterminate\") => void",
    },
    label: {
      description: "Optional label rendered beside the checkbox.",
      type: "ReactNode",
    },
    labelClassName: {
      description: "Additional CSS classes for the label text.",
      type: "string",
    },
    indicatorClassName: {
      description: "Additional CSS classes for the animated SVG checkmark indicator.",
      type: "string",
    },
    disabled: {
      description: "Whether the checkbox is disabled.",
      type: "boolean",
      default: "false",
    },
    className: {
      description: "Additional CSS classes merged onto the checkbox box element.",
      type: "string",
    },
  }}
/>

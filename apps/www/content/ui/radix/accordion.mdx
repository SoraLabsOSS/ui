---
title: Accordion
description: An animated accordion component built on Radix UI with Motion spring physics, height collapse mask gradient, blur transitions, and gliding layoutId focus ring.
author:
  name: Axyl
  url: https://github.com/Axyl1410
intent: Expand and collapse related content sections inline.
role: disclosure
a11yConstraints:
  - Keep each trigger keyboard accessible and associated with its panel.
  - Use headings and concise labels to preserve document structure.
  - Avoid hiding required task controls inside collapsed content.
motionEngine: Motion layout-height morphing with spring chevron and focus-ring transitions.
compositionRules:
  - Use one AccordionItem per independently discoverable section.
  - Prefer multiple mode for unrelated sections and single mode for stepwise disclosure.
compositionRecipes:
  - name: radix-faq-sections
    description: Group independently expandable answers in a content page.
    components:
      - radix/accordion
    constraints:
      - Keep trigger labels meaningful without relying on panel content.
---

<ComponentPreview name="demo-radix-accordion" description="Default animated Radix UI accordion with Motion spring transitions and glowing focus ring." />

## Installation

<ComponentInstallation name="radix-accordion" />

## Usage

```tsx
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@/components/sora-ui/radix/accordion";

export default function Page() {
  return (
    <Accordion defaultValue={["item-1"]}>
      <AccordionItem value="item-1">
        <AccordionTrigger>Is it accessible?</AccordionTrigger>
        <AccordionContent>
          Yes. It adheres to the WAI-ARIA design pattern using Radix UI primitives.
        </AccordionContent>
      </AccordionItem>
      <AccordionItem value="item-2">
        <AccordionTrigger>Is it animated?</AccordionTrigger>
        <AccordionContent>
          Yes! It features Motion spring physics, mask gradient height collapse,
          blur transitions, and gliding focus rings.
        </AccordionContent>
      </AccordionItem>
    </Accordion>
  );
}
```

## Examples

### Bordered Container

<ComponentPreview name="demo-radix-accordion-bordered" description="Accordion in a bordered container with refined surface styling." />

### Separated Cards

<ComponentPreview name="demo-radix-accordion-separated" description="Accordion with individual card-style separated items." />

### Multiple Items

<ComponentPreview name="demo-radix-accordion-multiple" description="Accordion allowing multiple panels to expand simultaneously." />

## Props

### Accordion

<TypeTable
  type={{
    value: {
      description: "The controlled value of the item(s) that should be expanded.",
      type: "string | number | (string | number)[]",
    },
    defaultValue: {
      description: "The uncontrolled value of the item(s) that should be initially expanded.",
      type: "string | number | (string | number)[]",
      default: "[]",
    },
    multiple: {
      description: "Whether multiple items can be open at the same time.",
      type: "boolean",
      default: "false",
    },
    variant: {
      description: "Visual style variant of the accordion.",
      type: '"default" | "bordered" | "separated" | "ghost"',
      default: '"default"',
    },
    showFocusRing: {
      description: "Whether to render the gliding animated focus ring on keyboard focus.",
      type: "boolean",
      default: "true",
    },
    disableAnimation: {
      description: "Whether to disable all motion physics and animations.",
      type: "boolean",
      default: "false",
    },
  }}
/>

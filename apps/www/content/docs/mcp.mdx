---
title: MCP
description: Connect the Sora UI docs MCP server to Cursor and other AI clients.
---

import { CodeTabs } from '@/components/docs/code-tabs';
import { AddToCursorButton } from '@/components/docs/add-to-cursor-button';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import {
  Tabs,
  TabsList,
  TabsTrigger,
  TabsContents,
  TabsContent,
} from '@/components/radix/tabs';
import {
  SORA_UI_MCP_SERVER_NAME,
  SORA_UI_MCP_URL,
} from '@/lib/mcp/cursor-install';

The **Sora UI docs MCP** server gives AI assistants live access to this site — installation guides, primitives, and the component catalog. It runs as a remote HTTP server at <code>{SORA_UI_MCP_URL}</code> (no local CLI required).

## Tools

| Tool | Description |
| --- | --- |
| `search_docs` | Search docs and components by keyword or phrase. Capped at 25 results per call — the response notes when results may be truncated, so narrow the query or raise `limit` |
| `get_page` | Fetch a page as markdown by slug (e.g. `primitives/accordion`, `primitives/draw-underline-link`) |
| `list_sections` | Browse the doc structure. Valid `section` filters: `documentation`, `components` |
| `list_components` | List installable Sora UI components and hooks with short descriptions — no need to configure a second MCP server just to browse |
| `get_component_info` | Get the install command, dependencies, and file targets for a component or hook by name |

<Callout>
  `list_components` and `get_component_info` let you browse and inspect the registry directly from **Sora UI docs MCP** — handy if you just want to look something up. To actually **install** `@soralabs/*` components with natural language, still configure the [shadcn MCP server](https://ui.shadcn.com/docs/mcp) in the same project (see [Registry MCP (shadcn)](#registry-mcp-shadcn) below).
</Callout>

## Add to Cursor

Cursor supports [one-click MCP install links](https://cursor.com/docs/mcp/install-links). Click below — Cursor opens a prompt to add the server, then enable it under **Settings → Tools & MCP**.

<div className="not-prose my-6 flex flex-wrap items-center gap-3">
  <AddToCursorButton />
</div>

<Steps>
<Step>

Click **Add to Cursor** and confirm the install dialog.

</Step>
<Step>

Open **Settings → Tools & MCP** and make sure **{SORA_UI_MCP_SERVER_NAME}** is enabled (green status).

</Step>
<Step>

Restart Cursor if tools do not appear, then try a prompt like *“Search Sora UI docs for accordion”*.

</Step>
</Steps>

## Manual configuration (Cursor)

Add this to `.cursor/mcp.json` in your project (or `~/.cursor/mcp.json` for all projects):

```json
{
  "mcpServers": {
    "sora-ui": {
      "url": "https://mcp.soralabs.io.vn"
    }
  }
}
```

After saving, enable the server in **Settings → Tools & MCP** and restart Cursor.

## Add to Claude Code

[Claude Code](https://claude.ai/code) is Anthropic's official CLI. Add the server with one command:

```bash
claude mcp add --transport http sora-ui https://mcp.soralabs.io.vn
```

<Steps>
<Step>

Run the command above in your terminal (inside or outside a project).

</Step>
<Step>

Verify the server was added with `claude mcp list` — you should see **sora-ui** in the list.

</Step>
<Step>

Start a new Claude Code session and try a prompt like *"Search Sora UI docs for accordion"*.

</Step>
</Steps>

### Manual configuration (Claude Code)

Add to `~/.claude/settings.json` (global) or `.claude/settings.json` (project):

```json
{
  "mcpServers": {
    "sora-ui": {
      "type": "http",
      "url": "https://mcp.soralabs.io.vn"
    }
  }
}
```

## Add to Claude Desktop

Open the Claude Desktop config file:

- **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`

Add the server under `mcpServers`:

```json
{
  "mcpServers": {
    "sora-ui": {
      "type": "http",
      "url": "https://mcp.soralabs.io.vn"
    }
  }
}
```

<Steps>
<Step>

Open the config file at the path for your operating system.

</Step>
<Step>

Add the `sora-ui` entry under `mcpServers` (create the file with the content above if it does not exist yet).

</Step>
<Step>

**Restart Claude Desktop** — the server connects automatically on the next launch.

</Step>
</Steps>

## Example prompts

### Browse documentation

- List all Sora UI documentation sections
- Search Sora UI docs for text reveal components
- Get the MCP documentation page from Sora UI

### Components catalog

- Search Sora UI components for underline link
- Get the draw-underline-link primitive page
- List the components section in Sora UI docs

### Registry browsing

- List installable Sora UI components
- Get install info for char-stagger-button
- What dependencies does the scroll-gallery component need?

## Registry MCP (shadcn)

To add `@soralabs/*` components from the registry with natural language, configure the shadcn MCP server alongside Sora UI docs MCP.

### Automatic configuration

<Tabs defaultValue="cursor">
  <TabsList>
    <TabsTrigger value="claude">Claude Code</TabsTrigger>
    <TabsTrigger value="cursor">Cursor</TabsTrigger>
    <TabsTrigger value="vscode">VS Code</TabsTrigger>
  </TabsList>
  <TabsContents>
    <TabsContent value="claude" className="h-[100px]">
      <CodeTabs
        lang="bash"
        codes={{
          pnpm: `pnpm dlx shadcn@latest mcp init --client claude`,
          npm: `npx shadcn@latest mcp init --client claude`,
          yarn: `yarn shadcn@latest mcp init --client claude`,
          bun: `bunx --bun shadcn@latest mcp init --client claude`,
        }}
      />
    </TabsContent>
    <TabsContent value="cursor" className="h-[100px]">
      <CodeTabs
        lang="bash"
        codes={{
          pnpm: `pnpm dlx shadcn@latest mcp init --client cursor`,
          npm: `npx shadcn@latest mcp init --client cursor`,
          yarn: `yarn shadcn@latest mcp init --client cursor`,
          bun: `bunx --bun shadcn@latest mcp init --client cursor`,
        }}
      />
    </TabsContent>
    <TabsContent value="vscode" className="h-[100px]">
      <CodeTabs
        lang="bash"
        codes={{
          pnpm: `pnpm dlx shadcn@latest mcp init --client vscode`,
          npm: `npx shadcn@latest mcp init --client vscode`,
          yarn: `yarn shadcn@latest mcp init --client vscode`,
          bun: `bunx --bun shadcn@latest mcp init --client vscode`,
        }}
      />
    </TabsContent>
  </TabsContents>
</Tabs>

### Manual configuration

Install the shadcn package as a dev dependency:

<CodeTabs
  lang="bash"
  codes={{
    pnpm: `pnpm add -D shadcn@latest`,
    npm: `npm install -D shadcn@latest`,
    yarn: `yarn add -D shadcn@latest`,
    bun: `bun add -D shadcn@latest`,
  }}
/>

Merge the shadcn entry into your MCP config (example for Cursor):

<CodeTabs
  lang="json"
  codes={{
    Cursor: `{
  "mcpServers": {
    "sora-ui": {
      "url": "https://mcp.soralabs.io.vn"
    },
    "shadcn": {
      "command": "npx",
      "args": ["shadcn@latest", "mcp"]
    }
  }
}`,
    'Claude Code': `{
  "mcpServers": {
    "sora-ui": {
      "type": "http",
      "url": "https://mcp.soralabs.io.vn"
    },
    "shadcn": {
      "command": "npx",
      "args": ["shadcn@latest", "mcp"]
    }
  }
}`,
    'VS Code': `{
  "servers": {
    "shadcn": {
      "command": "npx",
      "args": ["shadcn@latest", "mcp"]
    }
  }
}`,
  }}
/>

Register `@soralabs` in `components.json` before asking the agent to install components — see [Installation](/docs/installation).

## Troubleshooting

### Server not connecting

**Cursor**: Confirm **{SORA_UI_MCP_SERVER_NAME}** is enabled in **Settings → Tools & MCP**, restart Cursor after changing `.cursor/mcp.json`, and check **View → Output → MCP** for errors.

**Claude Code**: Run `claude mcp list` to verify the server is registered. If missing, re-run `claude mcp add --transport http sora-ui https://mcp.soralabs.io.vn`. Use `claude mcp get sora-ui` to inspect the saved config.

**Claude Desktop**: Confirm the JSON in `claude_desktop_config.json` is valid (no trailing commas), then fully quit and reopen the app.

### Tools return no results

- Use `list_sections` first to see available `documentation` and `components` sections
- For component pages, try slugs like `cursor-trail-reveal` or paths like `primitives/accordion`

### Install link does nothing

Paste the deeplink into your browser address bar, or add the [manual JSON config](#manual-configuration-cursor) above. See [Cursor MCP install links](https://cursor.com/docs/mcp/install-links).

### Nested `sora-ui` key in `mcp.json`

If Cursor shows *must have either a command or url* and your config looks like `"sora-ui": { "sora-ui": { "url": "..." } }`, remove the extra nesting — use the [manual JSON](#manual-configuration-cursor) shape, or click **Add to Cursor** again on this page (the install link encodes only `{ "url": "..." }`; the `name` query param supplies the server key).

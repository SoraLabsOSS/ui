---
title: Accordion
description: An animated FAQ accordion with expandable panels, powered by Motion. A React component for shadcn/ui.
author:
  name: Axyl
  url: https://github.com/Axyl1410
---

<ComponentPreview name="demo-accordion" description="Animated FAQ accordion showing expandable panels with smooth height transitions." />

## Installation

<ComponentInstallation name="accordion" />

## Usage

```tsx
import { Accordion } from '@/components/sora-ui/disclosure/accordion';

const items = [
  {
    title: 'What is Sora UI?',
    content:
      'Sora UI is an animated component registry for React and Next.js, built on Motion.',
    defaultOpen: true,
  },
  {
    title: 'How do I install a component?',
    content: 'Run npx shadcn@latest add @soralabs/accordion after initializing shadcn/ui.',
  },
];

export default function Page() {
  return <Accordion items={items} />;
}
```

For custom content per item, pass a React node as `content` or compose with the compound API:

```tsx
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from '@/components/sora-ui/disclosure/accordion';

export default function Page() {
  return (
    <Accordion>
      <AccordionItem defaultOpen>
        <AccordionTrigger>Custom layout</AccordionTrigger>
        <AccordionContent>
          <div className="pb-5 text-sm">Your own markup here.</div>
        </AccordionContent>
      </AccordionItem>
    </Accordion>
  );
}
```

## Props

### Accordion

<TypeTable
  type={{
    allowMultiple: {
      description: 'When false, opening one item closes the others.',
      type: 'boolean',
      default: 'false',
    },
    duration: {
      description: 'Panel expand/collapse duration in seconds.',
      type: 'number',
      default: '0.8',
    },
    iconMode: {
      description: 'Plus icon animation mode.',
      type: '"rotate" | "fade" | "both"',
      default: '"both"',
    },
    iconRotation: {
      description: 'Icon rotation in degrees when open.',
      type: 'number',
      default: '-180',
    },
    ease: {
      description: 'Easing curve for panel and icon animations.',
      type: '[number, number, number, number]',
      default: 'expo.inOut',
    },
    disabledOn: {
      description:
        'Skip expand/collapse animations on matching breakpoints (mobile, tablet, desktop). Items stay interactive.',
      type: '("mobile" | "tablet" | "desktop")[]',
    },
    items: {
      description:
        'Data-driven accordion items. Each entry needs title and content (string or React node).',
      type: '{ id?: string; title: string; content: React.ReactNode; defaultOpen?: boolean; enableStagger?: boolean }[]',
    },
    className: {
      description: 'Additional class names for the root container.',
      type: 'string',
    },
  }}
/>

### AccordionItem

<TypeTable
  type={{
    defaultOpen: {
      description: 'Whether this item starts expanded.',
      type: 'boolean',
      default: 'false',
    },
    delay: {
      description: 'Delay before panel animation starts, in seconds.',
      type: 'number',
      default: '0',
    },
    duration: {
      description: 'Override panel duration for this item, in seconds.',
      type: 'number',
    },
    ease: {
      description: 'Override easing curve for this item.',
      type: '[number, number, number, number]',
    },
    iconRotation: {
      description: 'Override icon rotation for this item, in degrees.',
      type: 'number',
    },
    disabledOn: {
      description:
        'Skip animations for this item on matching breakpoints. The trigger stays clickable.',
      type: '("mobile" | "tablet" | "desktop")[]',
    },
    enableStagger: {
      description:
        'Reveal panel text line by line when the item opens. Paragraphs are split into masked lines that slide up in sequence.',
      type: 'boolean',
      default: 'false',
    },
    staggerDuration: {
      description: 'Duration of each line reveal, in seconds.',
      type: 'number',
      default: '0.6',
    },
    staggerDelay: {
      description: 'Delay between each staggered line, in seconds.',
      type: 'number',
      default: '0.15',
    },
    staggerEase: {
      description: 'Easing curve for the line reveal.',
      type: '[number, number, number, number]',
      default: 'expo.out',
    },
    staggerYPercent: {
      description:
        'Initial vertical offset of each line, as a percentage of its height.',
      type: 'number',
      default: '110',
    },
    staggerStartDelay: {
      description:
        'Wait before the line reveal starts after opening, in milliseconds.',
      type: 'number',
      default: '200',
    },
    onOpen: {
      description: 'Callback fired when this item opens.',
      type: '() => void',
    },
    onClose: {
      description: 'Callback fired when this item closes.',
      type: '() => void',
    },
  }}
/>

## When to Use

Use the Accordion for FAQ sections, settings panels, or any content group where users need to scan multiple topics quickly and expand only the ones they care about. It works well when you have 4–12 items of similar importance and collapsing them reduces cognitive load — for example, product feature breakdowns, pricing tier details, or support documentation.

Avoid it when all items need to be visible at once, or when there are fewer than 3 items (a simple list or card layout is clearer).

## Accessibility

`AccordionTrigger` renders as a native `<button>`, so keyboard navigation (Tab, Enter, Space) works without extra configuration. Each panel region gets an `aria-expanded` attribute and is linked to its trigger via `aria-controls`. When `prefers-reduced-motion: reduce` is active, expand and collapse animations are skipped while the items remain fully interactive.

## Credits

<ComponentCredits name="accordion" />

---
title: Text Underline
description: A CSS-only link underline that slides through on hover. A React component.
author:
  name: Axyl
  url: https://github.com/Axyl1410
---

<ComponentPreview name="text-underline" description="CSS-only link underline that slides through on hover — hover each link to see the transition." />

## Installation

<ComponentInstallation name="text-underline" />

## Usage

```tsx
import { TextUnderline } from '@/components/sora-ui/texts/text-underline';

export default function Page() {
  return (
    <TextUnderline href="#" underlineColor="brand">
      Shop Performance Collection
    </TextUnderline>
  );
}
```

## Props

<TypeTable
  type={{
    children: {
      description: 'Link text content.',
      type: 'ReactNode',
    },
    label: {
      description: 'Alternative to `children` for demos and controlled previews.',
      type: 'string',
    },
    href: {
      description: 'Link destination.',
      type: 'string',
      default: '"#"',
    },
    underlineColor: {
      description:
        'Underline color preset (`currentColor`, `brand`, `brand-muted`, `gray`) or any valid CSS color.',
      type: 'string',
      default: '"currentColor"',
    },
    underlineHeight: {
      description: 'Underline thickness preset.',
      type: '"thin" | "medium" | "thick"',
      default: '"thin"',
    },
    duration: {
      description: 'Animation speed preset.',
      type: '"fast" | "normal" | "slow"',
      default: '"normal"',
    },
    initialScale: {
      description:
        'Initial underline width before hover, as a percentage of the link width.',
      type: '"0" | "25" | "50" | "75"',
      default: '"0"',
    },
    anchor: {
      description:
        '`left` slides the line through on hover; `center` expands from the middle.',
      type: '"left" | "center"',
      default: '"left"',
    },
    className: {
      description: 'Additional class names merged onto the root anchor.',
      type: 'string',
    },
  }}
/>

## When to Use

Use `TextUnderline` for inline links in articles, marketing pages, or anywhere the default browser underline feels too static. The slide-through animation signals interactivity without the overhead of a JavaScript animation library — it uses CSS `transform` only.

It works best at `text-base` or larger. At very small sizes the underline may be too thin to register as an animated element.

## Accessibility

`TextUnderline` renders as a standard `<a>` tag with an underline that is always visible (not hidden until hover). The sliding animation is a CSS `transform` on a pseudo-element and does not affect the link's accessible name, role, or keyboard behavior. Focus state adds the standard browser outline. There are no Motion dependencies; the draw animation is skipped via the Tailwind `motion-reduce:after:scale-x-100 motion-reduce:after:transition-none` utility, which only applies under `prefers-reduced-motion: reduce` — CSS transitions aren't disabled by browsers automatically, this is an explicit opt-in.

---
title: Text Reveal
description: A text reveal animation.
author:
  name: Axyl
  url: https://github.com/Axyl1410
---

<ComponentPreview name="text-reveal" />

## Installation

<ComponentInstallation name="text-reveal" />

## Usage

```tsx
import { TextReveal } from '@/components/sora-ui/texts/text-reveal';

export default function Page() {
  return <TextReveal text="Hello world" />;
}
```

## Props

<TypeTable
  type={{
    text: {
      description: 'The text content to animate.',
      type: 'string',
      default: '""',
    },
    as: {
      description: 'HTML tag used for the container element.',
      type: 'keyof JSX.IntrinsicElements',
      default: '"p"',
    },
    splitBy: {
      description: 'Split the text word-by-word or character-by-character.',
      type: '"words" | "characters"',
      default: '"words"',
    },
    delay: {
      description: 'Delay before the first unit starts animating, in seconds.',
      type: 'number',
      default: '0.5',
    },
    duration: {
      description: 'Duration of each unit\'s animation, in seconds.',
      type: 'number',
      default: '0.5',
    },
    staggerDelay: {
      description: 'Extra delay added between each successive word/character, in seconds.',
      type: 'number',
      default: '0.05',
    },
    blur: {
      description: 'Blur amount in pixels applied to units before they animate in.',
      type: 'number',
      default: '4',
    },
    yOffset: {
      description: 'Initial vertical offset in pixels (units slide up from this position).',
      type: 'number',
      default: '0',
    },
    once: {
      description: 'When true, the animation triggers only the first time the element enters the viewport.',
      type: 'boolean',
      default: 'true',
    },
    viewportMargin: {
      description: 'Intersection Observer margin controlling when the animation fires.',
      type: 'string',
      default: '"0px 0px -10% 0px"',
    },
    className: {
      description: 'Additional class names applied to the container element.',
      type: 'string',
    },
    unitClassName: {
      description: 'Additional class names applied to each animated span (word or character).',
      type: 'string',
    },
  }}
/>
